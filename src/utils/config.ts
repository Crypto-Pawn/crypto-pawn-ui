export const PROVIDER_CHAIN_ID = parseInt((import.meta.env.VITE_PROVIDER_CHAINID ?? 0).toString(),);
export const PROVIDER_POLLING_INTERVAL = parseInt((import.meta.env.VITE_PROVIDER_POLLING_INTERVAL ?? 5000).toString(),);
